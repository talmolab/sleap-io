name: sleap-io

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.9
  - ffmpeg
  - imageio
  - attrs
  - h5py
  - numpy
  - conda-forge::opencv
  - pip
  - pip:
    - "--editable=.[dev]"
